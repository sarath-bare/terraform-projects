# Terraform-samples
This repository is created to store our project code
# Terraform AWS Demo

This project provisions an S3 bucket using Terraform.

## Commands Used
```bash
terraform init
terraform plan
terraform apply
